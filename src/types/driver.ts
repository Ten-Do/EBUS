export interface IDriver {
  bus: string
  first_name: string
  Id: string
  last_name: string
  number: string
  route: string
}

export interface IDriverData {
  name: string
  rout: string
  bus: string
  // status: string
  number: string
}
